services:
  kindle-epub-sync:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kindle-epub-sync
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
    restart: unless-stopped
    command: ["uv", "run", "kindle-epub-sync", "schedule"]
